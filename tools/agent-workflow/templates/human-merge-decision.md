# Human Merge Decision for {{featureId}} - {{featureName}}

The workflow has reached the human approval gate.

Branch: `{{currentBranch}}`
Base: `{{baseBranch}}`
Expected commit: `{{expectedCommit}}`

## Safety Rules

{{safetyRules}}

{{primaryWorktreeRule}}

Agents must not execute remote-mutating commands automatically.

## Human-Only Commands

{{humanOnlyCommands}}
