# Data Model: Project Workspace View

ProjectWorkspace contains a project id, project name, and a list of ProjectWorkspaceSection entries.
Each section has an id, label, placeholder status, enabled flag, and placeholder marker.