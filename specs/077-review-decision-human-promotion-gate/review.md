# Review: 077-review-decision-human-promotion-gate

**Reviewed commit (round 6, pending)**: see round-by-round history — round 6 has not yet run at the time of this writing.
**Decision**: Pending (round 6 re-review required after the comprehensive-parity-audit fix commit below)

Six independent review rounds were run via `tools/agent-workflow`. Rounds 1-2 used Implementer = Claude CLI (fallback for the assigned Codex CLI role), Reviewer = Claude CLI, per `AGENTS.md`/`CLAUDE.md` role assignment. Rounds 3-6 used Implementer = Claude CLI (same fallback), Reviewer = Codex CLI (`--agent codex`), run to satisfy the project's exact-HEAD provenance rule after each fix commit — the exact-HEAD rule requires a fresh independent review of the literal commit being published, not merely confirmation that the underlying source is unchanged.

## Round-by-round history

| Round | Reviewed commit | Outcome | Notes |
|---|---|---|---|
| 1 (attempt 1) | `d3a092c` | Timed Out | 480000ms timeout exceeded; not a code finding, matches Spec 076 round-1 precedent. Retried with `--timeout-ms 900000`. |
| 1 (attempt 2) | `d3a092c` | Changes Requested | See findings below. P1-001 fixed in `d9f04f2`; P2-001 fixed in `d9f04f2`; P3-001 accepted as residual risk (documented below). |
| 2 | `d9f04f2` | Approved | Both round-1 findings confirmed resolved against their governing contracts; no new blocking findings. Three new low-severity notes surfaced (N-001, N-002, tautological-check note) — see below. |
| 3 (Codex, exact-HEAD) | `24b8c28` | Changes Requested | First independent Codex CLI review of this feature (previous rounds used Claude CLI as Reviewer). Surfaced a new P1 (`P1-001`, below) plus two new non-blocking doc-drift findings (`P3-001`/`P3-002`, distinct from round 1's `P3-001`). Structured `schemaVersion` correctly numeric `1` this round (round 2's `"1.0"`-string parser bug did not recur). |
| 4 (Codex, exact-HEAD, unchanged tree) | `24b8c28` | Changes Requested | Re-run of round 3 at the identical HEAD, at the user's request, to check reproducibility before triaging. Independently reproduced the same `P1-001` root claim (worded slightly differently, with a broader recommendation covering result/runtime linkage in addition to status/decision), plus the same `P3-001` non-blocking finding. Two independent Codex passes agreeing on the same blocking finding is why it was fixed rather than rejected — see P1-001 below. |
| 5 (Codex, exact-HEAD) | `4961ce7` | Changes Requested | Re-review against the HEAD produced by fixing rounds 3-4's finding. Surfaced a new `P1-001` (distinct from rounds 3-4's): `validateChain` still omitted Runtime Start/Implementer Runtime/Review Target `worktreePath`/`branch`/`specificationPath` linkage checks and the Review Target clean-working-tree guard, all of which `ReviewerRuntimeService.validateContext`/`validateReviewTarget` already enforce. This was the third distinct parity gap found across five rounds, so a one-time comprehensive field-by-field audit (not another single-field patch) was authorized before continuing — see "Comprehensive parity audit" below. |
| 6 (Codex, exact-HEAD) | pending | pending | To be run against the new HEAD after the comprehensive-parity-audit fix commit described below. |

## Blocking Findings

### Round 1 (commit `d3a092c`) — fixed in `d9f04f2`
- **P1-001** — `OfficeProjectPortalView.ts:348-357`: the `[REVIEW DECISION]` dashboard row was built by `createReviewDecisionDisplayRows` from only the latest `ReviewerRuntimeResult` and `ReviewPromotionCollection`, never calling `ReviewDecisionService.classify` or revalidating the plan/readiness/approval/preflight/runtimeStart/implementerRuntime chain. This contradicted `plan.md` and `contracts/review-decision-contract.md`, which require the dashboard render and the Promote precondition to read the identical classification, and could show a confident "Approved; Promote (P)" row on a chain `classify()` would have reported as Stale. **Fixed**: extracted `resolveReviewDecisionInput` in `ReviewDecisionService.ts` as the single shared assembly of `ReviewDecisionInput` from raw per-project stage collections; both `OfficeProjectPortalController.promoteReviewForPromotion` and `OfficeProjectPortalView.render` now call it and pass the result to `ReviewDecisionService.classify`, so the dashboard and the Promote precondition always classify from the same input. `ReviewDecisionView.createReviewDecisionDisplayRows` now takes a `ReviewDecisionClassification` directly instead of re-deriving its own narrower check from a raw `ReviewerRuntimeResultCollection`. Added regression coverage in `ReviewDecisionView.test.ts` for the Stale-classification and promotion-gone-stale cases.

  Deviated from the reviewer's literal `recommendation` (have the Controller compute `classify()` once and thread the result into `render()`): that would require changing `render(state)`'s signature across ~50 existing call sites, or introducing a "controller precomputes into state" pattern with no precedent elsewhere in the codebase. Every other dashboard row is instead computed by the View itself, purely from `state`, at render time (the "latest per project" pattern). Having the View call `classify()` directly follows that same existing pattern and fully satisfies the finding's substance (classify() must back the dashboard, using the same shared assembly as Promote) with a smaller, more consistent diff.

### Rounds 3-4 (commit `24b8c28`, Codex CLI) — fixed in the following commit
- **P1-001** — `ReviewDecisionService.ts:359-360` (`validateChain`): the final chain check verified only `reviewerRuntimeResult.reviewerRuntimeId === reviewerRuntime.reviewerRuntimeId`, never that `reviewerRuntimeResult.status`/`.decision` actually match `reviewerRuntime.status`/`.decision`. Independently reproduced by two separate Codex passes (round 3 and round 4, same HEAD, same root claim), which is why this was fixed rather than triaged as a one-off false positive.

  Reachability trace performed before deciding to fix: through `ReviewerRuntimeService.startReviewer`'s primary construction pipeline, a Runtime and Result sharing a `reviewerRuntimeId` are always written atomically/identically — either via the fresh "spawned" success path or via the existing-runtime idempotency mirror — so no divergence is reachable through that path today. The two controller-level branches that write synthetic Results directly (`OfficeProjectPortalController.startReviewerRuntimeForPromotion`'s "start-stale"/"active-block" branches) never set `reviewerRuntimeId` on those synthetic Results, so they can never match an existing Runtime's id either. No currently-reachable production path was found that produces the exact divergent state Codex describes.

  **Fixed anyway**, because the correct precedent is not round-2's `N-001` (truly unreachable, accepted) but round-1's `P2-001` (fixed): `validateChain`'s own docstring claims to mirror `ReviewerRuntimeService.validateContext` field-by-field, and that sibling function *does* check `implementerRuntime.status !== "Completed" || implementerRuntimeResult.status !== "Completed"` (runtime/result status parity) plus `implementerRuntimeResult.projectId !== plan.projectId` (result-side project linkage) before trusting the Implementer stage — checks `validateChain` was missing for the equivalent Reviewer stage. Two independent reviewers agreeing that a defense-in-depth check present on every other stage's sibling record is absent here, on the feature whose entire purpose is the human promotion gate's integrity, outweighs a reachability trace against today's call sites — a future refactor could introduce a new construction path the trace didn't anticipate.

  **Fix**: extended the final check in `validateChain` to also require `reviewerRuntimeResult.projectId === plan.projectId`, `reviewerRuntimeResult.status === reviewerRuntime.status`, and `reviewerRuntimeResult.decision === reviewerRuntime.decision`, returning the same existing `REVIEW_PROMOTION_REVIEWER_NOT_COMPLETED` reason code (no new reason code introduced). Four existing `classify`/`promote` tests that mutated only `reviewerRuntime`'s status/decision without updating the paired `reviewerRuntimeResult` (a test-fixture convenience, not a realistic scenario) were updated to keep both records in sync, matching the now-enforced invariant. Added two new regression tests (`ReviewDecisionService.test.ts`) proving a diverging Result now collapses classification to `Stale` and blocks Promote with `REVIEW_PROMOTION_REVIEWER_STALE`, rather than being silently trusted.

### Round 5 (commit `4961ce7`, Codex CLI) — fixed via comprehensive parity audit, see below
- **P1-001** — `ReviewDecisionService.ts:278-337`: promotion chain revalidation omitted material Runtime Start, Implementer Runtime, and Review Target fields — Runtime Start branch/spec/worktree linkage, Implementer Runtime branch/spec/worktree linkage, and Review Target branch/spec/worktree/`workingTreeState` linkage were all unchecked, unlike their equivalents in `ReviewerRuntimeService.validateContext`/`validateReviewTarget`. This was the third distinct parity gap found across rounds 1, 3-4, and 5 (see round-1 P2-001 and rounds 3-4 P1-001 above) — a pattern, not a one-off. Fixed via a one-time comprehensive audit rather than another single-field patch (see below).

## Comprehensive parity audit (Round 6 authorization)

Following round 5's third distinct parity-gap finding, one comprehensive field-by-field audit of `ReviewDecisionService.validateChain` against `ReviewerRuntimeService.validateContext`/`validateRoleBinding`/`validateReviewTarget` was authorized and performed, rather than fixing round 5's specific finding in isolation. Method and full field-by-field matrix (matches / intentionally differs / not applicable / missing) recorded in `.agent-workflow/spec-077-parity-audit.md` (gitignored working artifact, not part of the shipped product). Audit scope rule: mirror what the sibling validators actually check, not the full type surface of each record — e.g. `RuntimeStart.executionReadinessResultId` exists on the type but `validateContext` itself never checks it, so it was out of scope to add here too.

**Fields added to `validateChain`** (all reusing existing reason codes — no new reason code introduced):
- Runtime Start block (`REVIEW_PROMOTION_START_STALE`): `executionPlanId`, `humanExecutionApprovalId`, `runtimePreflightId`, `taskId`, `confirmedAssignmentId`, `preparedSessionId`, `activeSessionId`, `employeeId`, `worktreePath`, `repositoryRoot` (vs `plan.repositoryPath`), `branch`, `specificationPath`.
- Implementer Runtime block (`REVIEW_PROMOTION_IMPLEMENTER_NOT_COMPLETED`): `worktreePath`, `branch`, `specificationPath`, and a `reviewerStarted`/`validationStarted`/`githubMutationStarted`-all-false guard (verified: these fields are typed as literal `false` and never constructed as `true` anywhere in the codebase, so this addition is zero-risk parity, not a behavior change).
- Review Target block (`REVIEW_PROMOTION_TARGET_MISMATCH`): `worktreePath`, `featureBranch` (note: `ReviewTarget` uses `featureBranch`, not `branch`, unlike the other three record types in this chain), `specificationPath`, and `workingTreeState === "Clean"`.

**Intentional non-additions** (documented in the audit artifact with rationale): `ReviewTarget.baseSha`/`reviewTargetSha`/`mergeBaseSha`/`baseBranch` are not independently re-validated by `validateReviewTarget` itself either — there is no stored "expected" value to compare against at that layer, and `reviewTargetSha` is transitively bound anyway via the existing `reviewerRuntime.reviewTargetId === reviewTarget.reviewTargetId` check, since `createReviewTargetId` is deterministic on `(projectId, runtimeStartId, reviewTargetSha, rulesVersion)`. `RuntimeStart.validationCommands`/`.mutationScope` and `.executionReadinessResultId` are on the type but never compared by `validateContext` either. `command.approvedImplementerAgent`/`.approvedReviewerAgent` (validated in `validateRoleBinding`) are not applicable — `ReviewDecisionInput` has no "start command," since Review Decision revalidates an already-completed chain rather than gating a new start.

**No shared helper introduced.** Exporting from `reviewer-runtime/*` would violate the Constitution Check's "Reviewer Runtime services reused unmodified"; a new shared module would contradict the documented per-stage duplication precedent (`research.md` "Precedent-scope boundary", `plan.md` "Chain Revalidation", `review-decision-contract.md` Input Boundary). The duplication added here is intentional and precedented, matching every other per-stage `validateContext` copy in the codebase.

**Test fixture note**: adding the `workingTreeState === "Clean"` guard surfaced that `OfficeProjectPortalController.review-decision.test.ts`'s integration helper stores the real `resolveReviewTarget(...)` output (which always reports `"Uncommitted"` in this represented pipeline, matching the same documented limitation `OfficeProjectPortalController.reviewer-runtime.test.ts` already works around) as the project's current Review Target. The helper now forces that stored Review Target's `workingTreeState` to `"Clean"` after the driven chain reaches Completed/Approved, mirroring the sibling file's existing override pattern — this is a test-fixture fix, not a product-code workaround, since the underlying `resolveReviewTarget` behavior is an already-documented, out-of-scope architectural limitation of this synthetic pipeline.

**Regression tests added** in `ReviewDecisionService.test.ts`: a table-driven `it.each` covering all twelve new mismatch checks under `classify` (Runtime Start worktreePath/repositoryRoot/branch/specificationPath/executionPlanId; Implementer Runtime worktreePath/branch/specificationPath; Review Target worktreePath/featureBranch/specificationPath/workingTreeState), a dedicated Implementer Runtime flags-parity test, and five `promote`-level tests (dirty-tree block, Runtime Start branch mismatch block, no-mutation-of-source-runtime-on-block, valid-chain-still-promotes, project-isolation). All confirm: promotion is blocked, no promotion record is created, the source `reviewerRuntime`/`reviewerRuntimeResult` are unmutated, a valid exact chain still promotes, and idempotency/project-isolation remain intact.

## Non-Blocking Findings

### Round 1 (commit `d3a092c`)
- **P2-001** (fixed in `d9f04f2`) — `ReviewDecisionService.ts:220-350`: `validateChain` never compared `runtimeStart.repositoryId`, `implementerRuntime.repositoryId`, or `reviewTarget.repositoryId` against the current `plan.repositoryId`, unlike `ReviewerRuntimeService.validateContext`'s equivalent checks. Verified as a genuine parity gap (not a rejectable repo-wide-generalization request): grepped `ReviewerRuntimeService.validateContext` and confirmed it performs exactly this three-point `repositoryId` check (lines 316, 337, 416), and `validateChain`'s own docstring explicitly claims to mirror that function field-by-field. **Fixed**: added the three matching `repositoryId !== plan.repositoryId` checks, returning the same reason codes (`REVIEW_PROMOTION_START_STALE`, `REVIEW_PROMOTION_IMPLEMENTER_NOT_COMPLETED`, `REVIEW_PROMOTION_TARGET_MISMATCH`) already used by the adjacent checks for those same records. Added three regression tests in `ReviewDecisionService.test.ts` (`describe("Stale ...")` block), one per check.
- **P3-001** (accepted as residual risk, not fixed) — `OfficeProjectPortalController.ts:2246-2320`: `promoteReviewForPromotion` returns `true` regardless of `outcome.result.granted`, and the dashboard never surfaces a blocked promote attempt's reason code to the human. The reviewer's own `recommendation` explicitly frames this as "a follow-up UX improvement," not a defect in the feature's contract — the block itself is already correctly enforced by `promote()` and durably recorded in `reviewPromotionResultCollections`; only the inline reason-code readout is missing. Implementing it now would grow the dashboard row's contract and test surface beyond what this round's P1/P2 fixes required, in tension with this feature's explicit "do not broaden this feature into a general workflow engine" constraint. See Residual Risks below.

### Round 2 (commit `d9f04f2`) — Approved, no blocking findings

Confirmed both round-1 fixes resolved against their governing contracts (dashboard now shares `resolveReviewDecisionInput`/`classify()` with the Promote precondition; `validateChain` now matches `ReviewerRuntimeService.validateContext`'s `repositoryId` parity checks), with matching regression tests verified present. Full changed-file inventory (30 files) inspected; validation gate (`vitest`, `tsc --noEmit`, `npm run build`, `git diff --check`) clean; no push/PR/merge/GitHub-mutation/provider-invocation occurred during the review. Three new low-severity notes surfaced, all accepted as residual risk / no action:

- **N-001** — `REVIEW_PROMOTION_REVIEWER_MISSING` (`ReviewDecisionTypes.ts`) is unreachable from either call site: `classify()` short-circuits to `Unavailable` before `validateChain` runs when `reviewerRuntime` is absent, and `promote()`'s precondition 1 always intercepts with `TARGET_MISMATCH` first in that same case. Harmless defensive code; no test exercises it as an outcome. **Accepted, no action** — removing it is a pure cleanup with no behavioral effect, out of scope for this pass.
- **N-002** — `tasks.md` T015 described the pre-fix signature of `createReviewDecisionDisplayRows`. **Fixed**: T015's note updated to describe the actual post-fix signature (`ReviewDecisionClassification`, not `ReviewerRuntimeResultCollection`).
- **Tautological target-mismatch note** (not a defect) — `promoteReviewForPromotion` builds `request.reviewerRuntimeId` from the same `input.reviewerRuntime` that `promote()` immediately re-classifies against, so `TARGET_MISMATCH` can't fire through this exact call path; the check remains genuinely load-bearing at the service level via direct/independent test calls. **No action needed.**

### Rounds 3-4 (commit `24b8c28`, Codex CLI) — both fixed in the following commit
- **P3-001 (round 3/4, distinct from round 1's P3-001)** — `contracts/review-decision-contract.md:12`: the contract stated the upstream chain is "revalidated using the same functions `ReviewerRuntimeService.validateContext` already exposes/reuses — never a duplicated copy of that logic." Verified stale by direct inspection: `validateChain` is in fact its own private, duplicated copy, matching the repository's established per-stage convention documented in T001/research.md, not an import of `ReviewerRuntimeService.validateContext`. **Fixed**: contract line corrected to describe the actual mirror-not-import convention, citing T001/research.md and the `ImplementerRuntimeService`/`ReviewerRuntimeService` precedent.
- **P3-002** — `plan.md:112`: stated `clearRuntimePreflightForProject` "is extended to also delete both new collections," contradicting T009's documented FR-011 correction and the actual shipped code (the two Review Promotion collections are deliberately *not* deleted by that helper, so a recorded promotion survives as an immutable historical record). Verified stale by direct inspection of both `plan.md` and `tasks.md` T009. **Fixed**: paragraph corrected to describe the actual FR-011 immutability behavior, cross-referencing T009.

## Suggestions

- None outstanding beyond the accepted residual risks below.

## Residual Risks

- **Blocked-promote feedback (P3-001, round 1)**: a human pressing `P` on a Stale/blocked review sees no inline reason code distinguishing "blocked" from any other no-op; the reason is recorded in `reviewPromotionResultCollections` but not surfaced in the dashboard row. Low severity (P3, explicitly framed by the reviewer as a follow-up UX improvement, not a contract violation, confirmed unchanged and still non-blocking in rounds 2-4); a future pass could surface `outcome.result.reasonCodes` in the `[REVIEW DECISION]` row when `granted` is false.
- **Unreachable `REVIEW_PROMOTION_REVIEWER_MISSING` reason code (N-001)**: dead code, harmless, confirmed unreachable from both `classify()` and `promote()`'s actual control flow both before and after the P1-001 fix (the fix touched the final check in `validateChain`, not the `!reviewerRuntime` short-circuit `N-001` concerns). A future cleanup could remove it or annotate it as intentionally unreachable defensive code.
