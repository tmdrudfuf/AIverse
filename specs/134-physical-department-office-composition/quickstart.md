# Quickstart: Physical Department Office Composition

Run focused validation:

```powershell
npx vitest run src/features/city-view/scene/office/layout/OfficeLayoutService.test.ts
git diff --check
```

Full ADOS validation is deferred to ADOS per the implementation handoff.
