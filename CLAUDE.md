# Claude Instructions

This repository's canonical agent instructions live in `AGENTS.md`. Read it first.

Claude CLI is the default Reviewer role assignment and fallback Implementer when explicitly assigned. These are role assignments, not permanent product-specific architecture.

For automated local Implementer -> Reviewer -> fix loops, review the active Spec Kit plan linked from `AGENTS.md` and preserve the human-only boundary for push, PR readiness, approval, merge, and remote deletion.

Do not duplicate instructions here. Update `AGENTS.md` instead and keep this file as a pointer only.
